spring:
  application:
    name: conference-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      enabled: true
      retry:
        max-attempts: 6
        max-interval: 2000
